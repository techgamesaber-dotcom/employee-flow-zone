import { createServerFn } from "@tanstack/react-start";
import { z } from "zod";
const codeSchema = z.string().trim().min(2).max(40);
type WorkerRow = { id: string; name: string; code: string; is_admin: boolean };
async function admin() { const { supabaseAdmin } = await import("@/integrations/supabase/client.server"); return supabaseAdmin; }
async function resolveWorker(code: string): Promise<WorkerRow> { const db = await admin(); const { data, error } = await db.from("workers").select("id, name, code, is_admin").eq("code", code.trim().toUpperCase()).maybeSingle(); if (error) throw new Error(error.message); if (!data) throw new Error("Invalid code"); return data as WorkerRow; }
export const signInWithCode = createServerFn({ method: "POST" }).inputValidator((i: { code: string }) => ({ code: codeSchema.parse(i.code) })).handler(async ({ data }) => { const w = await resolveWorker(data.code); return { id: w.id, name: w.name, isAdmin: w.is_admin, code: w.code }; });
export const getWorkerDashboard = createServerFn({ method: "POST" }).inputValidator((i: { code: string }) => ({ code: codeSchema.parse(i.code) })).handler(async ({ data }) => { const w = await resolveWorker(data.code); const db = await admin(); const [a,o] = await Promise.all([db.from("attendance").select("id, day, status").eq("worker_id",w.id).order("day",{ascending:false}).limit(60),db.from("orders").select("id, customer_name, order_details, price, price_paid, price_left, created_at").eq("worker_id",w.id).order("created_at",{ascending:false}).limit(100)]); if(a.error)throw new Error(a.error.message);if(o.error)throw new Error(o.error.message);return {worker:{id:w.id,name:w.name,isAdmin:w.is_admin},attendance:a.data??[],orders:o.data??[]}; });
export const markAttendance = createServerFn({ method: "POST" }).inputValidator((i:{code:string;status:"present"|"absent";day?:string})=>({code:codeSchema.parse(i.code),status:z.enum(["present","absent"]).parse(i.status),day:z.string().regex(/^\d{4}-\d{2}-\d{2}$/).optional().parse(i.day)})).handler(async({data})=>{const w=await resolveWorker(data.code);const db=await admin();const day=data.day??new Date().toISOString().slice(0,10);const{error}=await db.from("attendance").upsert({worker_id:w.id,day,status:data.status},{onConflict:"worker_id,day"});if(error)throw new Error(error.message);return{ok:true as const,day,status:data.status};});
export const addOrder = createServerFn({method:"POST"}).inputValidator((i:{code:string;customerName:string;orderDetails:string;price:number;pricePaid:number})=>({code:codeSchema.parse(i.code),customerName:z.string().trim().min(1).max(120).parse(i.customerName),orderDetails:z.string().trim().min(1).max(500).parse(i.orderDetails),price:z.number().min(0).max(1e8).parse(i.price),pricePaid:z.number().min(0).max(1e8).parse(i.pricePaid)})).handler(async({data})=>{const w=await resolveWorker(data.code);const db=await admin();const{error}=await db.from("orders").insert({worker_id:w.id,customer_name:data.customerName,order_details:data.orderDetails,price:data.price,price_paid:Math.min(data.pricePaid,data.price)});if(error)throw new Error(error.message);return{ok:true as const};});
export const updateOrder = createServerFn({method:"POST"}).inputValidator((i:{code:string;id:string;customerName:string;orderDetails:string;price:number;pricePaid:number})=>({code:codeSchema.parse(i.code),id:z.string().uuid().parse(i.id),customerName:z.string().trim().min(1).max(120).parse(i.customerName),orderDetails:z.string().trim().min(1).max(500).parse(i.orderDetails),price:z.number().min(0).max(1e8).parse(i.price),pricePaid:z.number().min(0).max(1e8).parse(i.pricePaid)})).handler(async({data})=>{const w=await resolveWorker(data.code);const db=await admin();let q=db.from("orders").update({customer_name:data.customerName,order_details:data.orderDetails,price:data.price,price_paid:Math.min(data.pricePaid,data.price)}).eq("id",data.id);if(!w.is_admin)q=q.eq("worker_id",w.id);const{error}=await q;if(error)throw new Error(error.message);return{ok:true as const};});
export const deleteOrder = createServerFn({method:"POST"}).inputValidator((i:{code:string;id:string})=>({code:codeSchema.parse(i.code),id:z.string().uuid().parse(i.id)})).handler(async({data})=>{const w=await resolveWorker(data.code);const db=await admin();let q=db.from("orders").delete().eq("id",data.id);if(!w.is_admin)q=q.eq("worker_id",w.id);const{error}=await q;if(error)throw new Error(error.message);return{ok:true as const};});
export const getAdminDashboard = createServerFn({method:"POST"}).inputValidator((i:{code:string})=>({code:codeSchema.parse(i.code)})).handler(async({data})=>{const w=await resolveWorker(data.code);if(!w.is_admin)throw new Error("Not allowed");const db=await admin();const[wr,ar,or]=await Promise.all([db.from("workers").select("id,name,code,is_admin").order("name"),db.from("attendance").select("id,worker_id,day,status").order("day",{ascending:false}),db.from("orders").select("id,worker_id,customer_name,order_details,price,price_paid,price_left,created_at").order("created_at",{ascending:false})]);if(wr.error)throw new Error(wr.error.message);if(ar.error)throw new Error(ar.error.message);if(or.error)throw new Error(or.error.message);const workers=(wr.data??[]).filter(x=>!x.is_admin),attendance=ar.data??[],orders=or.data??[];const summary=workers.map(worker=>{const att=attendance.filter(a=>a.worker_id===worker.id),ord=orders.filter(o=>o.worker_id===worker.id);return{id:worker.id,name:worker.name,code:worker.code,present:att.filter(a=>a.status==="present").length,absent:att.filter(a=>a.status==="absent").length,todayStatus:att.find(a=>a.day===new Date().toISOString().slice(0,10))?.status??null,orderCount:ord.length,collected:ord.reduce((s,o)=>s+Number(o.price_paid),0),pending:ord.reduce((s,o)=>s+Number(o.price_left),0),total:ord.reduce((s,o)=>s+Number(o.price),0)};});const names=Object.fromEntries(workers.map(x=>[x.id,x.name]));return{admin:{name:w.name},summary,orders:orders.map(o=>({...o,workerName:names[o.worker_id]??"Unknown"})),totals:{total:orders.reduce((s,o)=>s+Number(o.price),0),collected:orders.reduce((s,o)=>s+Number(o.price_paid),0),pending:orders.reduce((s,o)=>s+Number(o.price_left),0),presentToday:summary.filter(s=>s.todayStatus==="present").length,absentToday:summary.filter(s=>s.todayStatus==="absent").length,workers:workers.length}};});
export const addWorker = createServerFn({method:"POST"}).inputValidator((i:{code:string;name:string;workerCode:string})=>({code:codeSchema.parse(i.code),name:z.string().trim().min(1).max(80).parse(i.name),workerCode:z.string().trim().min(3).max(20).regex(/^[A-Za-z0-9-]+$/).parse(i.workerCode)})).handler(async({data})=>{const w=await resolveWorker(data.code);if(!w.is_admin)throw new Error("Not allowed");const db=await admin();const{error}=await db.from("workers").insert({name:data.name,code:data.workerCode.toUpperCase()});if(error)throw new Error(error.message);return{ok:true as const};});
export const updateWorker = createServerFn({method:"POST"}).inputValidator((i:{code:string;workerId:string;name:string;workerCode:string})=>({code:codeSchema.parse(i.code),workerId:z.string().uuid().parse(i.workerId),name:z.string().trim().min(1).max(80).parse(i.name),workerCode:z.string().trim().min(3).max(20).regex(/^[A-Za-z0-9-]+$/).parse(i.workerCode)})).handler(async({data})=>{const w=await resolveWorker(data.code);if(!w.is_admin)throw new Error("Not allowed");const db=await admin();const{data:t,error:te}=await db.from("workers").select("id,is_admin").eq("id",data.workerId).maybeSingle();if(te)throw new Error(te.message);if(!t)throw new Error("Worker not found");if(t.is_admin)throw new Error("Admin accounts cannot be edited");const{error}=await db.from("workers").update({name:data.name,code:data.workerCode.toUpperCase()}).eq("id",data.workerId);if(error)throw new Error(error.message);return{ok:true as const};});
export const deleteWorker = createServerFn({method:"POST"}).inputValidator((i:{code:string;workerId:string})=>({code:codeSchema.parse(i.code),workerId:z.string().uuid().parse(i.workerId)})).handler(async({data})=>{const w=await resolveWorker(data.code);if(!w.is_admin)throw new Error("Not allowed");const db=await admin();const{data:t,error:te}=await db.from("workers").select("id,is_admin").eq("id",data.workerId).maybeSingle();if(te)throw new Error(te.message);if(!t)throw new Error("Worker not found");if(t.is_admin)throw new Error("Admin accounts cannot be deleted");const{error}=await db.from("workers").delete().eq("id",data.workerId);if(error)throw new Error(error.message);return{ok:true as const};});
